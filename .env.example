# Configuración de entorno para la aplicación
# Copie este archivo a .env y ajuste los valores según su entorno

# Modo de la aplicación
# - 'demo': Usa datos mock para desarrollo/demostración (default)
# - 'production': Conecta a la API real
VITE_APP_MODE=demo

# URL base de la API (solo se usa en modo production)
VITE_API_BASE_URL=http://localhost:3001/api

# Tiempo de expiración de sesión en minutos
VITE_SESSION_TIMEOUT=60
